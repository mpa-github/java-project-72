# Generate in memory H2 database for tests
ebean:
  test:
    useDocker: false
    platform: h2
    ddlMode: dropCreate
    dbName: test
  #ddl:
    #run: true
    #generate: true
    #seedSql: seed-test-db.sql
